import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  picker: {
    width: 75,
    borderColor: 'black',
    borderWidth: 1,
    height: 44,
    overflow: 'hidden'
  }
});

export default styles;